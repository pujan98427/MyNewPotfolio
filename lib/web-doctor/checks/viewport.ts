import { recommendation } from "../recommendations";import type { SeoCheck } from "../types/analysis";
export const checkViewport:SeoCheck=({document})=>{const viewport=document.viewport.trim(),responsive=/\bwidth\s*=\s*device-width\b/i.test(viewport);return {id:"viewport",label:"MOBILE VIEWPORT",status:viewport&&responsive?"pass":"warning",value:viewport||"Missing",explanation:!viewport?"No viewport metadata was detected. Mobile browsers may render the page using a wider desktop-style viewport.":responsive?"The page declares a device-width viewport for responsive mobile rendering.":"Viewport metadata is present, but it does not declare width=device-width. Review whether the page will use the device’s actual viewport width.",fix:viewport&&responsive?undefined:recommendation("viewport")};};
