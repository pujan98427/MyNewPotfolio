import { recommendation } from "../recommendations";import type { SeoCheck } from "../types/analysis";
import { safeFetchImagePreview } from "../safe-fetch";
export const checkOpenGraph:SeoCheck=async ({document,url,manual})=>{const title=document.openGraph["og:title"]??"",description=document.openGraph["og:description"]??"",image=document.openGraph["og:image"]??"",ogUrl=document.openGraph["og:url"]??"",type=document.openGraph["og:type"]??"",siteName=document.openGraph["og:site_name"]??"",count=[title,description,image].filter(Boolean).length;let preview=null;if(image&&!manual){try{preview=await safeFetchImagePreview(new URL(image,url));}catch{}}const complete=count===3,previewAvailable=Boolean(preview);return {id:"social",label:"OPEN GRAPH",status:complete&&(previewAvailable||manual)?"pass":count===0?"error":"warning",value:`${count}/3 core tags found`,explanation:complete&&manual?"The core Open Graph values were detected. Image retrieval is deliberately disabled during manual HTML analysis.":complete&&previewAvailable?"The core Open Graph values and a safely bounded preview image are available.":image&&!preview?"The image URL was unsafe, inaccessible, unsupported, or larger than the protected preview limit. A fallback is shown.":"Shared links may have incomplete or unpredictable previews.",fix:complete?undefined:recommendation("social"),openGraphAnalysis:{title,description,image,url:ogUrl,type,siteName,imagePreview:preview?.dataUrl,imageStatus:!image?"missing":preview?"available":"unsafe-or-inaccessible"}};};
