import { recommendation } from "../recommendations";import type { SeoCheck } from "../types/analysis";
export const checkTitle:SeoCheck=({document})=>{const value=document.title,length=Array.from(value).length;const needsReview=Boolean(value)&&(length<15||length>70);return {id:"title",label:"TITLE",status:!value?"error":needsReview?"warning":"pass",value:value||"Not found",explanation:!value?"The title helps search engines and users understand what this page is about.":needsReview?"Your title is present, but it may be worth reviewing for clarity. Length ranges are guidance, not ranking rules.":"Your title is present and descriptive.",fix:!value||needsReview?recommendation("title"):undefined};};
