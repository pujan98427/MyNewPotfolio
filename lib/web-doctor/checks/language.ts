import { recommendation } from "../recommendations";import type { SeoCheck } from "../types/analysis";
export const checkLanguage:SeoCheck=({document})=>{const language=document.language.trim(),valid=/^[a-z]{2,3}(?:-[a-z0-9]{2,8})*$/i.test(language);return {id:"language",label:"DOCUMENT LANGUAGE",status:language&&valid?"pass":"warning",value:language||"Missing",explanation:!language?"The document does not declare its primary language. Adding it helps assistive technology choose appropriate pronunciation and gives search systems clearer language context.":valid?`The document declares ${language} for assistive technology and search systems.`:"A lang attribute is present, but its value does not look like a valid language tag. Review it for accessibility and language targeting.",fix:language&&valid?undefined:recommendation("language")};};
