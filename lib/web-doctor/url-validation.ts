export function normalizeWebsiteUrl(input:string){const candidate=input.trim();if(!candidate)throw new Error("Enter a website URL to analyse.");const hasExplicitScheme=/^[a-z][a-z0-9+.-]*:/i.test(candidate);let url:URL;try{url=new URL(hasExplicitScheme?candidate:`https://${candidate}`);}catch{throw new Error("Enter a valid website address, such as example.com.");}if(!["http:","https:"].includes(url.protocol))throw new Error("Only HTTP and HTTPS websites can be analysed.");if(url.username||url.password)throw new Error("URLs containing login credentials are not supported.");const expectedPort=url.protocol==="https:"?"443":"80";if(url.port&&url.port!==expectedPort)throw new Error("Only standard web ports 80 and 443 can be analysed.");url.hash="";return url;}
