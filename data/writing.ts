export type Article={slug:string;toolSlug:string;title:string;excerpt:string;date:string;readingTime:string;sections:readonly {heading:string;body:string}[]};
export const articles:readonly Article[]=[
{slug:"what-web-doctor-checks-before-a-page-ships",toolSlug:"web-doctor",title:"What Web Doctor checks before a page ships",excerpt:"A practical look at the public HTML, metadata and technical signals Web Doctor inspects—and where its diagnosis stops.",date:"2026-08-31",readingTime:"7 min read",sections:[
{heading:"Why I built the check",body:"A page can look finished while its title is missing, its canonical points elsewhere, or its robots settings prevent indexing. Web Doctor puts those public signals into one readable report so a developer or site owner can find important omissions before release."},
{heading:"What the report inspects",body:"The analyser reads one requested HTML page, that origin’s robots.txt, and a declared or common sitemap location. Its isolated checks cover titles, descriptions, canonicals, indexability, headings, Open Graph and Twitter fields, image alternatives, links, structured data, language, viewport, charset, site identity, content landmarks, and selected response security headers."},
{heading:"How the score is organised",body:"Fixed weights cover discoverability, metadata, page structure, social sharing, accessibility basics, and technical basics. Passes receive the configured weight, warnings receive half, and errors receive none. A neutral check is not penalised. This is a diagnostic summary of the displayed checks, not a Google ranking score."},
{heading:"How a website is fetched safely",body:"Fetching happens on the server. Only HTTP and HTTPS on standard ports are accepted. DNS results are checked against private, loopback, link-local, reserved, multicast, and internal ranges, and every redirect is validated again. Requests also have timeouts, redirect limits, response-size limits, and content-type checks."},
{heading:"Why the advice is deterministic",body:"Each finding comes from an explicit check and reusable recommendation copy. No AI invents advice at runtime. Independent modules share one parsed document and fail separately, so a sitemap problem does not erase the metadata and structure results."},
{heading:"When manual investigation still matters",body:"Web Doctor reads the initial public HTML rather than running a browser, so JavaScript-rendered metadata may be absent. It does not crawl a whole site, prove search eligibility, measure real-user performance, or replace Search Console. Use the report as a lead, then inspect source, deployment settings, platform tools, and search-engine reports when the cause needs deeper investigation."}]},
{slug:"writing-page-metadata-without-guessing",toolSlug:"meta-generator",title:"Writing page metadata without guessing",excerpt:"How the Meta Tag Generator helps draft a clear title and description without turning character counts into ranking rules.",date:"2026-08-31",readingTime:"4 min read",sections:[
{heading:"Start with the page, not a keyword list",body:"A useful title names the page clearly and distinguishes it from nearby pages. A useful description tells a potential visitor what they will find. The generator keeps both fields together so their wording can be judged as one introduction."},
{heading:"What the live preview shows",body:"As the URL, title, and description change, the tool updates a neutral search-style preview and an HTML snippet. The preview helps expose repetition, vague language, and a title that makes little sense beside its URL."},
{heading:"Length is guidance",body:"Character feedback is an editing aid, not a ranking formula. There is no exact length that guarantees a particular display. Search engines can truncate text or create a different snippet based on the query and page content."},
{heading:"A practical drafting pass",body:"Name the page’s specific purpose first, add the site identity only when it helps recognition, and write the description naturally. Read the result beside the URL, remove repeated phrases, then copy the generated title and description markup into the page head or framework metadata configuration."},
{heading:"What the tool does not promise",body:"The generator does not analyse a live page, predict rankings, submit anything to a search engine, or guarantee that a description will be displayed. Use Web Doctor after publishing to confirm what the server returns."}]},
{slug:"checking-colour-contrast-before-shipping-a-ui",toolSlug:"contrast-checker",title:"Checking colour contrast before shipping a UI",excerpt:"A focused method for comparing foreground and background colours—and understanding what a passing ratio cannot prove.",date:"2026-08-31",readingTime:"4 min read",sections:[
{heading:"Compare the colours people actually see",body:"The checker accepts foreground and background hex colours, converts both to relative luminance, and reports their contrast ratio. Use resolved colours from the real interface; transparency, imagery, and layered surfaces can alter the final pixels."},
{heading:"Reading the current results",body:"The tool reports WCAG-related thresholds for AA normal text, AA large text, and AAA normal text. The labels remain separate because a ratio that passes for large text does not automatically pass for smaller copy."},
{heading:"Where contrast checks help",body:"Test body text, labels, navigation, buttons, form hints, and status messages. Repeat the check for hover, focus, pressed, disabled, error, and visited states whenever those states use different colours."},
{heading:"A ratio is not the whole interface",body:"Passing a mathematical threshold does not establish that an interface is accessible. Text size and weight, focus visibility, semantics, reading order, zoom, motion, wording, and using more than colour to communicate meaning still require review."},
{heading:"Use the result as an early gate",body:"Contrast is cheapest to correct while selecting tokens. Check candidate pairs before they spread through components, then verify the finished interface at real sizes and in the states people encounter."}]},
{slug:"using-css-clamp-without-doing-the-maths-every-time",toolSlug:"clamp-generator",title:"Using CSS clamp without doing the maths every time",excerpt:"How bounded fluid values work, what the generator calculates, and when a breakpoint remains the clearer choice.",date:"2026-08-31",readingTime:"5 min read",sections:[
{heading:"What a fluid value does",body:"A fluid CSS value changes gradually with the viewport instead of jumping at a breakpoint. clamp() combines a minimum, a preferred fluid calculation, and a maximum so the property responds without crossing chosen limits."},
{heading:"The four inputs",body:"The generator asks for minimum and maximum sizes in pixels plus the narrowest and widest viewport widths. It calculates a linear slope and intercept between those boundaries, then emits a clamp expression with rem limits and a viewport-width term."},
{heading:"A typography example",body:"For type that should be 16px at a 320px viewport and 48px at 1440px, enter those values and copy the generated font-size declaration. Resize the preview to inspect intermediate widths rather than assuming the mathematical line also produces good wrapping."},
{heading:"The same idea works for spacing",body:"The generated value can be moved from font-size to another length property such as gap, padding, or margin. Keep the bounds deliberate: fluid spacing should support the composition rather than grow simply because more screen is available."},
{heading:"When fixed values are better",body:"Use a fixed value when a property should not respond to viewport width. Use a breakpoint when the layout changes structurally, or when available space depends on a component container. clamp() reduces some media queries; it does not replace responsive design decisions."}]},
{slug:"checking-a-sharing-preview-before-publishing",toolSlug:"open-graph-preview",title:"Checking a sharing preview before publishing",excerpt:"A practical way to review Open Graph copy and image intent while remembering that platforms control the final card.",date:"2026-08-31",readingTime:"4 min read",sections:[
{heading:"What Open Graph fields describe",body:"Open Graph metadata gives a sharing crawler a page title, description, canonical URL, image URL, and site name. Together they help a link identify its destination away from the original website."},
{heading:"What this preview helps reveal",body:"The tool places the entered site, title, description, and image reference into a restrained card and produces matching meta tags. It can expose vague titles, repeated wording, an incorrect domain, or a missing absolute image URL before publishing."},
{heading:"Why the image is not fetched",body:"The current preview displays the supplied image reference but does not download or proxy it. That avoids fetching arbitrary URLs. Confirm the real public image separately and make sure sharing crawlers can reach it."},
{heading:"The final card can differ",body:"Platforms use different layouts, crop images at different ratios, cache earlier metadata, and may omit or rewrite fields. Keep essential image content away from edges and treat this tool as an approximation rather than a reproduction of a particular network."},
{heading:"Check again after publishing",body:"Copy the generated tags, deploy them in the document head, then inspect the live HTML. Web Doctor can report the Open Graph fields returned by the page; a platform’s own debugger is the right place to refresh that platform’s cache when available."}]},
{slug:"when-i-use-readable-svg-and-when-i-use-base64",toolSlug:"svg-base64-converter",title:"When I use readable SVG and when I use Base64",excerpt:"Choosing between an SVG file, readable markup, Base64, and a URL-encoded data URI without making the asset harder to maintain.",date:"2026-08-31",readingTime:"5 min read",sections:[
{heading:"SVG starts as readable markup",body:"An SVG is an XML document describing vector shapes, paths, and text. Keeping it as a normal file makes the source inspectable, independently cacheable, and easy to replace. That is usually the clearest default for artwork reused across pages."},
{heading:"What Base64 changes",body:"Base64 represents the SVG’s UTF-8 bytes with a restricted text alphabet. The converter uses TextEncoder and TextDecoder so Unicode, emoji, non-Latin text, and symbols survive the round trip. It accepts raw Base64 or an SVG Base64 data URI when decoding."},
{heading:"When embedding is useful",body:"A data URI can suit a small, self-contained icon inside CSS, an HTML image source, or an environment where a separate request is awkward. The tool provides raw Base64, a complete data URI, a CSS background declaration, a CSS url() value, and an img tag."},
{heading:"When Base64 is the wrong trade-off",body:"Encoding makes source larger and less readable, and embedded data cannot be cached as a separate file. Large illustrations, assets shared across pages, and artwork edited independently are usually better kept as normal optimized SVG files."},
{heading:"URL-encoded SVG is another option",body:"For CSS, a URL-encoded SVG data URI can remain more recognisable and may be smaller than Base64. The converter exposes that format under its advanced output so both representations can be compared."},
{heading:"Conversion stays in the browser",body:"Encoding, decoding, formatting, size calculations, and preview preparation run locally. Files and pasted SVG are not uploaded. The preview uses a separate sanitized version that removes active or external content; conversion output still represents the original source."}]}
];
export function getArticle(slug:string){return articles.find(article=>article.slug===slug);}
export function getArticleForTool(toolSlug:string){return articles.find(article=>article.toolSlug===toolSlug);}
