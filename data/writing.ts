export type Article = { slug:string; title:string; excerpt:string; date:string; readingTime:string; sections:readonly { heading:string; body:string }[] };
export const articles: readonly Article[] = [
  { slug:"interfaces-that-feel-fast", title:"Interfaces that feel fast", excerpt:"Performance is measured in milliseconds, but perceived speed is shaped by hierarchy, feedback, and restraint.", date:"2026-08-18", readingTime:"4 min read", sections:[{ heading:"Speed has a visual language", body:"A fast response can still feel slow when the interface gives no acknowledgement. Immediate state changes, stable layout, and a clear hierarchy make progress legible without adding decorative motion." },{ heading:"Spend movement carefully", body:"Animation is most useful when it explains continuity or confirms an action. When every element performs, nothing feels responsive; the page simply feels busy." },{ heading:"Build the fast path first", body:"Server-render durable content, reserve space for media, and ship interaction code only where the user can actually interact. The best performance work is often subtraction." }] },
  { slug:"editorial-grids-for-the-web", title:"Editorial grids for the web", excerpt:"A grid is not a cage. Used well, it creates enough order for asymmetry to feel deliberate.", date:"2026-07-29", readingTime:"5 min read", sections:[{ heading:"Begin with relationships", body:"Columns matter less than the relationships they create: title to image, caption to edge, dense passage to open field. Those relationships should survive across breakpoints even when the column count changes." },{ heading:"Tension needs a baseline", body:"Asymmetry reads as intentional when type, spacing, and alignment provide a dependable baseline. Without that underlying order, unusual composition becomes noise." },{ heading:"Let mobile be its own edit", body:"A desktop composition should not simply collapse. Mobile needs a fresh editorial decision about sequence, emphasis, and what deserves the first screen." }] }
];
export function getArticle(slug:string) { return articles.find((article) => article.slug === slug); }
