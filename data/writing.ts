import type { WebGuideSlug } from "@/data/web-guides";

export type Article={slug:string;toolSlug:string;title:string;excerpt:string;date:string;readingTime:string;relatedGuideSlugs?:readonly WebGuideSlug[];sections:readonly {heading:string;body:string}[]};
export const articles:readonly Article[]=[
{slug:"what-web-doctor-checks-before-a-page-ships",toolSlug:"web-doctor",title:"What Web Doctor checks before a page ships",excerpt:"A practical look at the public HTML, metadata, and technical signals Web Doctor reads, including the limits of its report.",date:"2026-08-31",readingTime:"7 min read",relatedGuideSlugs:["canonical-urls","robots-txt","structured-data"],sections:[
{heading:"A finished design can still hide basic problems",body:"A page may look ready while its title is missing or its canonical URL points somewhere else. It may even ask search engines not to index it. These details sit in the HTML and response headers, so they are easy to miss during a visual review. I built Web Doctor to put those public signals in one report. You enter a URL and the tool shows what it found, why a result may matter, and what to check next. It does not ask you to copy source code unless the website blocks automated access."},
{heading:"What the report reads",body:"Web Doctor requests one public page. It parses that HTML once and shares the parsed result with separate checks. It also requests the origin’s robots.txt file and looks for a sitemap declared there. If none is declared, it checks /sitemap.xml and /sitemap_index.xml. It does not follow every link or crawl the whole website. The report covers the title, meta description, canonical, robots instructions, headings, social metadata, image alt text, links, structured data, document language, viewport, charset, icons, page landmarks, and selected response headers."},
{heading:"How the checks are grouped",body:"The report separates findings into discoverability, metadata, page structure, social sharing, accessibility basics, and technical basics. Security headers appear in their own informational area because they should not be presented as a search ranking score. Each check returns a visible status and plain explanation. A missing description can be important and easy to fix. A missing touch icon is much less urgent. The interface keeps those differences visible so a small site-identity detail does not look as serious as a noindex instruction."},
{heading:"What the score means",body:"The score comes from weights stored in code. Discoverability, metadata, and page structure each have twenty available points. Social sharing has fifteen, accessibility basics has fifteen, and technical basics has ten. A pass receives the configured weight, a warning receives half, and an error receives none. Some findings are neutral when they do not apply or cannot be checked. The total is a quick summary of the checks shown in that report. It is not a Google score, a ranking prediction, or proof that a page will be indexed."},
{heading:"How the server fetch is limited",body:"The browser does not request the target website directly. A protected server route accepts HTTP and HTTPS URLs on the normal web ports. Before each request, the server resolves the hostname and rejects private, loopback, link-local, reserved, multicast, and internal addresses. It repeats that validation for every redirect. The fetch has a ten-second total timeout, a shorter connection timeout, a five-redirect limit, and an HTML limit of about 3 MB. Supporting files have smaller limits. The response must identify itself as HTML before the page is parsed."},
{heading:"Why one failed check does not remove the report",body:"Each diagnostic module runs inside its own error boundary. If sitemap fetching fails, title and heading checks can still finish. The same parsed page is reused, so the analyser does not download the HTML for every check. Independent checks run together where that is safe. Recommendation text also comes from typed, reusable rules. No AI writes the diagnosis at runtime. Given the same fetched document and rule configuration, the tool follows the same scoring and recommendation path."},
{heading:"When to investigate by hand",body:"Web Doctor reads the initial HTML response. It does not run the page in a browser, so metadata added only after JavaScript executes may not appear. It cannot tell whether Google has indexed a URL, whether a structured-data feature meets every current search requirement, or how real visitors experience performance. It also avoids fetching every external link. Use the report to identify a concrete lead. Then inspect the delivered source, deployment settings, Search Console, a platform sharing debugger, or browser accessibility tools when the question goes beyond the public HTML."}]},
{slug:"writing-page-metadata-without-guessing",toolSlug:"meta-generator",title:"Writing page metadata without guessing",excerpt:"How to draft a clear title and description without treating character counts as ranking rules.",date:"2026-08-31",readingTime:"5 min read",relatedGuideSlugs:["title-tags","meta-descriptions"],sections:[
{heading:"The usual problem is vague copy",body:"A page title often starts as a browser-tab label and gets little attention before release. The result may be only a brand name, a repeated heading, or wording that does not identify the page. A meta description can have the same problem. It may list broad search terms without telling anyone what the page contains. The Meta Tag Generator keeps the URL, title, and description together so you can read them as a person might encounter them outside the site."},
{heading:"Write the title from the page’s purpose",body:"Start with the specific job of the page. A contact page, product page, article, and service page need different wording because they answer different questions. Add the site or organisation name when it helps recognition, but do not let it replace the topic. Compare the draft with nearby pages. If several titles could be swapped without anyone noticing, they are probably too general. The tool counts characters while you type and shows the current title in a neutral search-style preview."},
{heading:"Describe what a visitor will find",body:"The description should make sense to somebody who has not opened the page. Say what the page contains and who it is for when that information is useful. Avoid promises the page cannot support. The generator updates the preview beside the title and URL, which makes repeated phrases easier to spot. It also creates the matching title and meta-description markup for you to copy. The copied code is a starting point for the document head or the metadata system used by your framework."},
{heading:"Character counts are editing guidance",body:"The tool marks a title of up to sixty characters as a good length. For descriptions, it treats 120 to 160 characters as a useful drafting range. Those values help keep the preview readable, but they are not ranking requirements. Search displays vary by device, query, and the width of individual characters. A search engine may shorten the text or create another snippet from the page. Keep the clearest wording even when it does not land on an exact number."},
{heading:"A simple review before copying",body:"Read the URL, title, and description aloud as one result. Check that the title names this page and that the description adds information instead of repeating it. Remove filler such as vague claims about quality. Confirm that the page itself supports every statement. After publishing, inspect the HTML returned by the server. Web Doctor can read the live title and description, which is useful when a framework setting, template, or cache prevents the new values from appearing."},
{heading:"What the generator cannot tell you",body:"This tool does not analyse a live URL or submit metadata to a search engine. It does not predict clicks or rankings. It cannot guarantee that a supplied description will appear in a search result. Its job is smaller: it gives you editable fields, immediate length feedback, a representative preview, and copyable HTML. The final check still belongs on the published page."}]},
{slug:"checking-colour-contrast-before-shipping-a-ui",toolSlug:"contrast-checker",title:"Checking colour contrast before shipping a UI",excerpt:"A focused way to compare foreground and background colours, with clear limits on what a passing ratio proves.",date:"2026-08-31",readingTime:"5 min read",sections:[
{heading:"Test the pair that reaches the screen",body:"Design tokens can look clear in a palette and fail when they meet in a component. The Colour Contrast Checker compares a text colour with the background directly beneath it. You can use the colour pickers or enter six-digit hex values. The preview updates with the same pair, and the result shows the calculated contrast ratio. If the interface uses transparency, a gradient, or an image, first work out the final colours visible at the point you are testing."},
{heading:"How the ratio is calculated",body:"The tool converts each sRGB colour to relative luminance. It then compares the lighter value with the darker value using the WCAG contrast formula. The order of the colours does not change the ratio, although their roles in the design still matter. The displayed number is rounded to two decimal places. This is useful when reviewing a token change because it shows the actual distance between the colours instead of relying on how they look on one monitor."},
{heading:"Reading the pass labels",body:"The current interface reports AA for normal text at 4.5:1, AA for large text at 3:1, and AAA for normal text at 7:1. These are separate results. A pair that passes the large-text line may still fail for body copy. Size and weight affect whether text qualifies as large, so check the rendered component as well as the ratio. The tool does not currently report every WCAG case, such as graphical objects or component boundaries."},
{heading:"Check states, not only the default",body:"A button may pass until its hover colour changes. A form hint may be readable while the disabled label is not. Test navigation, links, input labels, helper text, errors, and status messages. Repeat the comparison for focus, hover, pressed, selected, disabled, and visited states when they use different colours. If text sits over an image, test the lightest and darkest areas it can cross or provide a solid surface behind it."},
{heading:"A passing ratio is one check",body:"Contrast does not prove that a whole interface is accessible. People still need a clear focus indicator, correct HTML, useful labels, sensible reading order, keyboard access, and text that survives zoom. Colour should not be the only way to communicate an error or selected state. A green success message, for example, still needs words or another clear cue. Use the ratio as an early gate, then test the finished interface in context."}]},
{slug:"using-css-clamp-without-doing-the-maths-every-time",toolSlug:"clamp-generator",title:"Using CSS clamp without doing the maths every time",excerpt:"How bounded fluid values work, what the generator calculates, and when a breakpoint is clearer.",date:"2026-08-31",readingTime:"6 min read",sections:[
{heading:"Too many small jumps make sizing hard to tune",body:"Responsive type often starts with one size for mobile and another for desktop. More breakpoints get added when the change feels abrupt. CSS clamp() can replace some of those jumps with a value that changes gradually. It takes a minimum, a preferred value, and a maximum. The browser uses the preferred calculation while it stays inside the limits. Once it reaches either boundary, the value stops growing or shrinking."},
{heading:"The generator uses four real values",body:"Enter the smallest and largest sizes you want, then enter the viewport widths where those sizes should be reached. The tool expects pixels for all four inputs. It finds the slope between the two points and calculates the intercept needed for a straight line. The result uses rem for the lower and upper limits and a rem plus vw expression in the middle. If the viewport range is reversed or the maximum size is below the minimum, the tool shows an error instead of producing misleading CSS."},
{heading:"A type example",body:"Suppose a heading should be 16px at 320px and 48px at 1440px. Enter 16 and 48 for the sizes, then 320 and 1440 for the viewports. The generated declaration can be copied as font-size. Between those widths, the value rises along the calculated line. Below 320px it stays at the minimum. Above 1440px it stays at the maximum. The live preview responds when the browser width changes, so you can watch the result before using it."},
{heading:"The formula still needs a layout check",body:"A correct calculation can produce poor typography. Check line length, wrapping, hierarchy, and zoom at several widths. A display heading may need a lower maximum when a long word starts to overflow. Body copy often needs a narrower range than decorative type. The generated limits use rem to support zoom better than fixed pixel limits, but viewport units still affect the middle part. Test the real text instead of relying on the sample preview."},
{heading:"Using the value for spacing",body:"The copied output starts as a font-size declaration, but the clamp value can be used for gap, padding, margin, or another length property. Choose limits based on the relationship you want to preserve. A section gap may grow across a wide range. A button’s inner padding may need only a small change. Fluid spacing is most useful when the composition benefits from gradual movement and the minimum and maximum remain sensible."},
{heading:"When a breakpoint is the better tool",body:"Use a fixed value when the property should stay constant. Use a media query when the design changes state, such as moving navigation into a menu or changing a grid from two columns to one. Container queries may fit a component whose size depends on its parent instead of the viewport. clamp() handles bounded interpolation. It does not understand content, available container space, or where a layout needs a structural change."}]},
{slug:"checking-a-sharing-preview-before-publishing",toolSlug:"open-graph-preview",title:"Checking a sharing preview before publishing",excerpt:"A practical way to review Open Graph copy and image intent while remembering that platforms control the final card.",date:"2026-08-31",readingTime:"5 min read",relatedGuideSlugs:["open-graph"],sections:[
{heading:"A shared link needs enough context",body:"A page may be clear when somebody is already on the website but vague when it appears in a message or social feed. Open Graph tags give a sharing crawler a title, description, page URL, image URL, and site name. The Open Graph Preview puts those fields together so you can check whether the card identifies the destination. It also creates the corresponding meta tags for the document head."},
{heading:"Write for somebody outside the website",body:"Use a title that still makes sense without the navigation or page heading around it. The description should add a short explanation, not repeat the title word for word. Enter the canonical public URL rather than a staging address. For the image field, use an absolute HTTPS URL that a crawler can request without signing in. The tool also shows the hostname from the page URL, which can expose a copied development domain before publication."},
{heading:"What the image area currently does",body:"The preview displays the image URL as a reference. It does not download, proxy, or validate the image. This is deliberate because accepting arbitrary URLs would require another protected server fetch. Check the image directly in your deployment and confirm that it returns a supported image response. A landscape image around 1200 by 630 pixels is a common starting point, but each platform decides how it crops and displays that file."},
{heading:"Why the preview is approximate",body:"There is no single universal social card. Platforms use their own type sizes, spacing, image ratios, and fallback rules. They may shorten a description or ignore it. They can also cache an older version after the page changes. The neutral preview checks the relationship between your fields. It does not copy a specific network interface or promise that a final card will match it. Keep important subjects away from image edges so different crops remain usable."},
{heading:"Publish, inspect, and refresh",body:"Copy the tags and place them in the page head through HTML or your framework metadata system. Then inspect the public HTML response to make sure the new values are present. Web Doctor can show the Open Graph fields it reads from that response. If one platform still shows old content, use its official sharing debugger or cache refresh tool when available. Changing the draft in this preview cannot clear a third-party cache."},
{heading:"What the tool does not check",body:"The tool does not upload an image, test image file size, verify access permissions, or contact a social platform. It does not know whether a network will choose Open Graph fields, another metadata format, or content from the page. Use it to prepare and review the tags, then confirm the published result with the systems that will read them."}]},
{slug:"when-i-use-readable-svg-and-when-i-use-base64",toolSlug:"svg-base64-converter",title:"When I use readable SVG and when I use Base64",excerpt:"Choosing between an SVG file, readable markup, Base64, and a URL-encoded data URI without making the asset harder to maintain.",date:"2026-08-31",readingTime:"6 min read",sections:[
{heading:"A normal SVG file is often the simplest choice",body:"SVG is readable XML that describes vector shapes, paths, text, and paint. A normal .svg file is easy to inspect and replace. The browser can cache it separately, and several pages can use the same URL. I keep that form when an asset is shared, likely to change, or large enough that embedding it would make the stylesheet or HTML harder to read. Base64 is an option for specific constraints, not a default upgrade."},
{heading:"What Base64 does to the source",body:"Base64 converts the SVG’s UTF-8 bytes into a restricted set of text characters. It does not optimize the drawing or make the image sharper. The result is usually larger than the original markup. This converter shows the original byte size, encoded byte size, percentage difference, and character count so that cost is visible. It uses TextEncoder and TextDecoder, which means Unicode text, emoji, non-Latin characters, and symbols can survive an encode and decode round trip."},
{heading:"When an embedded value is useful",body:"A data URI can suit a small icon used in one CSS rule, a self-contained HTML example, or a template where adding a separate asset is awkward. The tool creates raw Base64 and a complete data:image/svg+xml;base64 URI. It also provides an img tag, a CSS background-image declaration, and a CSS url() value. Copy the form expected by the field you are editing. Raw Base64 alone will not work where a browser expects the full data URI prefix."},
{heading:"Decoding an existing value",body:"Switch to Base64 to SVG and paste raw Base64 or a complete SVG Base64 data URI. The tool removes a recognised prefix, validates the Base64 characters, decodes the UTF-8 bytes, and checks for a complete SVG root. It then shows formatted markup that can be copied or downloaded as an .svg file. The browser limit is 1 MB for the SVG source. Invalid input stays in the editor with an explanation instead of being replaced."},
{heading:"URL-encoded SVG can be easier in CSS",body:"Base64 is predictable, but a URL-encoded SVG data URI can remain partly readable and may use fewer characters. The advanced output compacts whitespace between tags, changes double quotes to single quotes, and percent-encodes characters that need it. It provides both the complete CSS background declaration and the url() function. Test the copied value in the target browser and build pipeline because minifiers and string escaping can affect embedded markup."},
{heading:"The preview uses a safer copy",body:"SVG can contain scripts, event attributes, external references, and foreignObject content. The preview never injects the supplied markup with dangerouslySetInnerHTML. It parses a separate copy, removes active or external elements and unsafe attributes, then loads the cleaned result through an image data URI. If anything is removed, the interface says so. This cleaning affects only the preview. The encoded output still represents the source you supplied, so you must treat decoded SVG from another person as untrusted code."},
{heading:"The conversion stays on the device",body:"Encoding, decoding, formatting, file measurements, and preview preparation run in the browser. Selecting a file does not upload it to this site. There is no account or saved conversion history. That makes the tool suitable for ordinary local assets, but it does not remove the need to review unfamiliar SVG before adding it to a project. Keep a normal file when caching and maintenance matter more than having one copy-and-paste value."}]},
{slug:"making-images-smaller-without-uploading-them",toolSlug:"image-compressor",title:"Making images smaller without uploading them",excerpt:"How browser image compression changes format and quality, and why the smallest file is not always the best result.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"Large images slow down ordinary tasks",body:"A photograph straight from a phone can be much larger than a website, email or form needs. The compressor reads the selected file in the browser, draws it to a canvas and creates a new copy. The default WebP output and practical quality setting often reduce photographic files without asking the user to understand codecs."},
{heading:"Quality changes the encoded copy",body:"JPEG and WebP use lossy compression. Lower quality usually produces fewer bytes, but fine texture, text and sharp edges may become less clear. PNG uses lossless encoding and ignores the quality control. The tool shows the processed preview and real output size so the decision is based on the file it actually created."},
{heading:"The original is never replaced",body:"The browser creates a separate downloadable result. It does not modify the source file on disk. Keep the original, inspect important details at full size and download only when the new copy suits its destination. A negative percentage means the result is smaller; a positive value means the chosen format increased it."},
{heading:"Transparency and format matter",body:"WebP and PNG can preserve transparency. JPEG cannot, so transparent areas receive a white background when JPEG is selected. Logos and interface graphics may remain sharper as PNG or SVG, while photographs usually respond better to WebP or JPEG. Changing formats is not automatically an improvement."},
{heading:"Processing stays local",body:"The file is decoded and encoded with browser APIs. It is not uploaded to this site, stored in an account or sent through an image service. Browser support determines which source formats can be read and which output formats can be created."}]},
{slug:"resizing-images-without-distorting-them",toolSlug:"image-resizer",title:"Resizing images without distorting them",excerpt:"A simple explanation of pixel dimensions, aspect ratio and what happens when a browser creates a resized copy.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"Dimensions and file size are different",body:"Width and height describe the pixel grid. File size describes how many bytes the encoded image uses. Reducing dimensions often reduces file size because fewer pixels remain, but the format and image content also matter. The resizer reports both the new dimensions and resulting bytes."},
{heading:"One dimension controls the other",body:"When you change width, the tool calculates the matching height from the original aspect ratio. Changing height does the reverse. This prevents a portrait from becoming unnaturally wide or a circle from turning into an oval. The values can still be edited before processing."},
{heading:"Choose dimensions for the destination",body:"A small profile image does not need the pixel dimensions of a full-screen photograph. Start with the largest size at which the image will actually appear, then account for high-density displays if needed. Avoid repeatedly resizing an already reduced copy because every new encoding can lose detail."},
{heading:"Upscaling cannot restore detail",body:"The tool can create a larger pixel grid, but it cannot recover information that was absent from the source. Enlarged images may look soft. For a larger result, return to the highest-quality original rather than scaling up a thumbnail."},
{heading:"The resize happens in the browser",body:"Canvas performs the resize locally and creates a new downloadable image. The selected file is not uploaded. Animated images are treated as a single rendered frame, so use a dedicated animation editor when motion must be preserved."}]},
{slug:"choosing-between-webp-jpeg-and-png",toolSlug:"image-format-converter",title:"Choosing between WebP, JPEG and PNG",excerpt:"What changes during browser image conversion and how to choose a sensible output format.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"Format should follow the image",body:"JPEG remains useful for photographs and broad browser compatibility. PNG is a good fit for lossless graphics and transparency. WebP handles photographs and transparency with efficient compression in current browsers. The converter offers these three outputs because they cover common web and office tasks."},
{heading:"Conversion is a new encoding",body:"The browser decodes the source into pixels, draws those pixels to a canvas and encodes a new file. Metadata such as camera details is not carried across. Animated sources become a still frame. Converting a JPEG to PNG does not restore detail already removed by JPEG compression."},
{heading:"Transparency needs attention",body:"PNG and WebP can keep transparent pixels. JPEG cannot. When JPEG is selected, the tool places white behind transparent areas instead of allowing them to become black or unpredictable. Check logos, shadows and cut-out product images carefully in the preview."},
{heading:"Compare bytes and appearance",body:"A newer format can produce a smaller result, but not for every image. Flat graphics, noisy photographs and screenshots behave differently. Review the output size and the visible result. Keep the format that meets the destination's support, clarity and size requirements."},
{heading:"Nothing is uploaded",body:"Reading, drawing and encoding happen with local browser APIs. The selected image is not sent to a conversion server. Available input support depends on the browser, so an uncommon format may need a desktop editor before it can be converted here."}]},
{slug:"cropping-images-for-common-layouts",toolSlug:"image-cropper",title:"Cropping images for common layouts",excerpt:"How aspect ratios change framing and how the browser cropper chooses which part of an image remains.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"Cropping changes the frame",body:"A crop removes pixels outside a chosen rectangle. It does not squeeze the full source into a different shape. The tool provides square, landscape, wide and portrait ratios that correspond to common cards, thumbnails and page sections."},
{heading:"Aspect ratio defines the shape",body:"A 1:1 crop has equal width and height. A 16:9 crop is much wider than it is tall. The browser calculates the largest rectangle of that ratio that fits inside the source, then removes the extra width or height."},
{heading:"Position decides what remains",body:"The crop-position control moves the calculated frame across the dimension with extra space. Use it to keep a face, object or piece of text inside the result. It is deliberately a simple one-axis control rather than a complex editor."},
{heading:"Leave important content breathing room",body:"A mathematically correct crop can still feel cramped. Keep faces, text and focal objects away from the edge when the destination may apply another crop. Review the actual output rather than relying on the ratio label alone."},
{heading:"The source stays on the device",body:"Canvas creates a separate cropped image in the browser. The original file is not changed or uploaded. Keep the source so another crop can be made later without repeatedly encoding an already processed copy."}]},
{slug:"joining-pdf-files-in-the-browser",toolSlug:"pdf-merger",title:"Joining PDF files in the browser",excerpt:"How the PDF Merger copies pages into one local document and what to check before sharing the result.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"Order is the main decision",body:"The merger accepts up to twenty PDF files and lists them in the order they will appear. Use the arrow controls to move a file before creating the result. Every page from the first file is copied before every page from the next."},
{heading:"Pages are copied, not photographed",body:"The local PDF library copies page objects into a new document. It does not render each page as an image, so selectable text and vector artwork can remain intact. Existing PDF features may vary, and encrypted files cannot be merged without their credentials."},
{heading:"The original files remain separate",body:"Merging creates a new download called merged.pdf. It does not alter or delete the selected files. Keep those originals until the combined document has been opened and checked in the reader used by its recipient."},
{heading:"Check page order and orientation",body:"Open the downloaded PDF and scan page boundaries, rotation and order. A merger does not standardise paper sizes or orientations. An A4 portrait page and a landscape presentation slide can coexist in the same result."},
{heading:"Documents stay in the browser",body:"Each selected PDF is read into browser memory and the combined file is created locally. No document is uploaded to this site. Very large files may exceed the memory available on a phone, so desktop processing is more reliable near the stated limits."}]},
{slug:"what-browser-pdf-compression-can-change",toolSlug:"pdf-compressor",title:"What browser PDF compression can change",excerpt:"A clear account of structural PDF compression, its limits and why some files cannot become smaller safely.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"PDF size has several causes",body:"A PDF can contain text, fonts, vector paths, photographs, scanned pages, forms and metadata. No single setting can reduce all of them without trade-offs. This tool focuses on how the document objects are stored rather than lowering image resolution."},
{heading:"The tool re-saves compact objects",body:"The browser loads the document and writes it with PDF object streams enabled. That can remove inefficient structural overhead in some files. It does not rasterize pages or deliberately reduce embedded image quality, so a scan dominated by JPEG images may barely change."},
{heading:"The smaller file wins",body:"Re-saving can occasionally increase a document. The tool compares the actual byte counts and keeps the original when the candidate is not smaller. The status explains this instead of presenting a larger file as successful compression."},
{heading:"When another approach is needed",body:"A scan may need image downsampling or optical character recognition. A design export may need font subsetting or different source settings. Those tasks require more control and can affect quality, searchability or accessibility, so they are outside this lightweight browser tool."},
{heading:"Private local processing has limits",body:"The PDF stays in browser memory and is not uploaded. Encrypted, damaged or unusually complex files may not load. Keep the original and open the downloaded result before relying on it, especially when forms, signatures or specialised PDF features matter."}]},
{slug:"creating-a-qr-code-that-remains-usable",toolSlug:"qr-code-generator",title:"Creating a QR code that remains usable",excerpt:"How QR content, contrast, output size and real-device testing affect a code people can scan.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"The code stores exactly what you enter",body:"A QR code can contain a web address, plain text, contact detail or another short value. The generator does not shorten, validate or visit a URL. Check spelling and include the complete HTTPS address when the code should open a website."},
{heading:"Shorter content is easier to scan",body:"More characters require a denser grid. Dense codes need more printed space and a clearer camera view. Keep the destination concise when possible. The tool limits input length to prevent an impractically dense result."},
{heading:"Contrast and quiet space matter",body:"The generated code uses near-black modules on white with a surrounding margin. Avoid recolouring it into a low-contrast combination or cropping away the empty border. Both help a scanner distinguish the code from its surroundings."},
{heading:"PNG and SVG suit different outputs",body:"PNG is convenient for documents and image editors. SVG stays sharp at different sizes and suits print or layout software that supports vectors. Choose a larger PNG when it will be printed, rather than enlarging a tiny raster file later."},
{heading:"Always test the final version",body:"Generation happens locally and does not confirm the destination. Scan the downloaded code with more than one device after placing it in the actual design. Printing material, glare, size and surrounding artwork can affect a code that worked on screen."}]},
{slug:"how-the-random-picker-chooses-an-item",toolSlug:"random-picker",title:"How the Random Picker chooses an item",excerpt:"How choices are parsed, how a secure browser random value selects one, and what fairness still depends on.",date:"2026-09-01",readingTime:"4 min read",sections:[
{heading:"The input becomes a simple list",body:"Each non-empty line or comma-separated value becomes one choice. Leading and trailing spaces are removed. Duplicate entries remain duplicates, which means repeating a name gives it more than one position in the list."},
{heading:"Selection uses secure browser randomness",body:"The picker asks crypto.getRandomValues for an unsigned random number. It rejects values outside an even range before converting the number to a list index. This avoids the small bias that a direct modulo operation can introduce."},
{heading:"Every list position has the same chance",body:"With one copy of each choice, every position has equal probability on each pick. The tool does not remember past results unless the optional removal setting is enabled. Independent picks can select the same item more than once."},
{heading:"Removing a winner changes later odds",body:"When removal is enabled, all entries exactly matching the selected text are removed after a pick. The next selection uses the remaining list. Leave the option off when repeat winners are allowed or when the same label intentionally appears several times."},
{heading:"Fair input still matters",body:"A sound random calculation cannot correct a biased list. Review duplicates, missing names and eligibility before picking. Choices stay in the browser and are not uploaded, but this lightweight utility is not an audited lottery or regulated drawing system."}]}
];
export function getArticle(slug:string){return articles.find(article=>article.slug===slug);}
export function getArticleForTool(toolSlug:string){return articles.find(article=>article.toolSlug===toolSlug);}
