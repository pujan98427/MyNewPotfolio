import { PageIntro } from "@/components/ui/page-intro"; import { ProjectCard } from "@/components/work/project-card"; import { projects } from "@/data/projects"; import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"Selected Frontend Work",description:"A concise overview of frontend and WordPress contributions completed by Pujan Chapagain as part of employment teams.",path:"/work"});
export default function WorkPage(){ return <main id="main"><PageIntro index="02" eyebrow="Work" title="Selected contributions, shown with context." description="A visual record of frontend work completed as part of employment teams. Product ownership, business results and internal implementation details are intentionally not presented here." /><section className="section project-list">{projects.map((project,index)=><ProjectCard key={project.slug} project={project} index={index} priority={index===0} />)}</section></main>; }
