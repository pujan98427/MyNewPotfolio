import { PageIntro } from "@/components/ui/page-intro"; import { ProjectCard } from "@/components/work/project-card"; import { projects } from "@/data/projects"; import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"Selected Frontend Work",description:"Explore selected frontend product, publishing, commerce and WordPress case studies by Pujan Chapagain.",path:"/work"});
export default function WorkPage(){ return <main id="main"><PageIntro index="02" eyebrow="Work" title="Selected work, considered in context." description="Shipped interfaces built across product applications, publishing systems, commerce, and personal brands." /><section className="section project-list">{projects.map((project,index)=><ProjectCard key={project.slug} project={project} index={index} priority={index===0} />)}</section></main>; }
