import { ToolFrame } from "@/components/lab/tool-frame"; import { MetaTool } from "@/components/lab/meta-tool"; import { ToolEducation } from "@/components/lab/tool-education"; import { getToolEducation } from "@/data/tool-education"; import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"Free Meta Tag Generator",description:"Draft an HTML title and meta description, review practical length guidance and preview a possible search result before copying the markup.",path:"/lab/meta-generator"});
export default function MetaGeneratorPage(){const education=getToolEducation("meta-generator")!;return <ToolFrame title="Meta generator" description="Shape a concise title and description, then copy the resulting markup." path="/lab/meta-generator"><MetaTool /><ToolEducation content={education} /></ToolFrame>; }
