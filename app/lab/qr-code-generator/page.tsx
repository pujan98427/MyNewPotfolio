import {QrCodeTool} from "@/components/lab/qr-code-tool";import {ToolPageLayout} from "@/components/lab/tool-page-layout";import {createToolPageMetadata} from "@/lib/seo/metadata";
export const metadata=createToolPageMetadata({title:"Free QR Code Generator",description:"Create a QR code from text or a URL and download it as PNG or SVG. Generated locally in your browser.",path:"/lab/qr-code-generator"});
export default function Page(){return <ToolPageLayout title="QR Code Generator" description="Create a QR code for a website, text, Wi-Fi network, email, phone, SMS, WhatsApp or contact and download it immediately." path="/lab/qr-code-generator"><QrCodeTool/><section className="qr-static-explanation" aria-labelledby="qr-static-heading"><p className="eyebrow">How it works</p><h2 id="qr-static-heading">The information lives inside the QR image.</h2><div><p>The generated QR code stores the information directly inside the image. It does not require an account, redirect service or connection to this tool after download.</p><p>The QR image itself does not expire. If it contains a website address, that destination must still continue to exist for the code to remain useful.</p><p>This tool does not create an editable destination, scan analytics or a tracking redirect. Test the downloaded image with a phone before printing or publishing it.</p></div></section></ToolPageLayout>}
