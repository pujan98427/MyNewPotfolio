import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import { PageIntro } from "@/components/ui/page-intro"; import { labTools } from "@/data/lab-tools"; import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"Free Web Tools — The Lab",description:"Use free, privacy-conscious tools for website diagnosis, metadata, colour contrast, responsive CSS and social sharing.",path:"/lab"});
export default function LabPage(){ return <main id="main"><PageIntro index="03" eyebrow="Lab" title="Useful things for the web." description="Small tools I build for developers, designers and website owners." /><section className="section lab-list">{labTools.map(tool=><Link href={`/lab/${tool.slug}`} key={tool.slug}><span>{tool.number}</span><div><small>{tool.category}</small><h2>{tool.title}</h2><p>{tool.description}</p></div><ArrowUpRight aria-hidden="true" /></Link>)}</section></main>; }
