import { ToolFrame } from "@/components/lab/tool-frame"; import { ContrastTool } from "@/components/lab/contrast-tool"; import { ToolEducation } from "@/components/lab/tool-education"; import { getToolEducation } from "@/data/tool-education"; import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"Colour Contrast Checker",description:"Check foreground and background colour contrast ratios against WCAG AA and AAA thresholds with immediate readable results.",path:"/lab/contrast-checker"});
export default function ContrastCheckerPage(){const education=getToolEducation("contrast-checker")!;return <ToolFrame title="Contrast checker" description="Test a colour pair against WCAG contrast thresholds in real time." path="/lab/contrast-checker"><ContrastTool /><ToolEducation content={education} /></ToolFrame>; }
