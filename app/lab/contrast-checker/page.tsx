import { ToolPageLayout } from "@/components/lab/tool-page-layout"; import { ContrastTool } from "@/components/lab/contrast-tool"; import { createToolPageMetadata } from "@/lib/seo/metadata";
export const metadata=createToolPageMetadata({title:"Colour Contrast Checker",description:"Check foreground and background colour contrast ratios against WCAG AA and AAA thresholds with immediate readable results.",path:"/lab/contrast-checker"});
export default function ContrastCheckerPage(){return <ToolPageLayout title="Contrast checker" description="Test a colour pair against WCAG contrast thresholds in real time." path="/lab/contrast-checker" variant="product"><ContrastTool /></ToolPageLayout>;}
