import { experience } from "@/data/experience";
import { ArrowUpRight } from "lucide-react";

export function ExperienceList() { return <div className="timeline">{experience.map((item,i)=>{const content=<><span>{String(i+1).padStart(2,"0")}</span><time>{item.period}</time><div className="experience-company"><h3>{item.company}</h3><p>{item.place}</p></div><div className="experience-role"><strong>{item.role}</strong><p>{item.detail}</p><ul aria-label={`Technologies used at ${item.company}`}>{item.technologies.map(technology=><li key={technology}>{technology}</li>)}</ul></div>{"url" in item&&<ArrowUpRight className="experience-arrow" aria-hidden="true" />}</>;return <article key={item.company} data-linked={"url" in item||undefined}>{"url" in item?<a className="experience-row" href={item.url} target="_blank" rel="nofollow noopener noreferrer" aria-label={`Visit the public ${item.company} website in a new tab`}>{content}</a>:<div className="experience-row">{content}</div>}</article>;})}</div>; }
