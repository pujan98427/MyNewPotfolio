import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import { labTools } from "@/data/lab-tools"; import { Breadcrumbs } from "@/components/navigation/breadcrumbs"; import { JsonLd } from "@/components/seo/json-ld"; import { webApplicationStructuredData } from "@/lib/seo/structured-data";
export function ToolFrame({ title, description, path, children, immersive=false }:{ title:string; description:string; path:string; children:React.ReactNode; immersive?:boolean }){ const tool=labTools.find(item=>`/lab/${item.slug}`===path); const schema=webApplicationStructuredData({name:title,description,path,category:tool?.category??"Web application"}); return <main id="main"><section className={`tool-page${immersive?" tool-page-immersive":""}`}><Breadcrumbs items={[{label:"Lab",href:"/lab"},{label:title,href:path}]} />{!immersive&&<header><p className="eyebrow">Frontend lab</p><h1>{title}</h1><p>{description}</p></header>}{children}<nav className="tool-switcher" aria-label="Other lab tools">{labTools.map(tool=><Link href={`/lab/${tool.slug}`} key={tool.slug}><span>{tool.number}</span>{tool.title}<ArrowUpRight aria-hidden="true" /></Link>)}</nav><JsonLd data={schema} /></section></main>; }
